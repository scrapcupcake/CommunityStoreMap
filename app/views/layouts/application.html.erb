<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for?(:title) ? yield(:title) : "Hand Cannon Community" %></title>
    <%= stylesheet_link_tag "application", "jquery.jnotify" %>
    <%= javascript_include_tag :defaults, "jquery.jnotify", "error_popups", "selectivizr" %>
    <%= csrf_meta_tag %>
    <%= yield(:head) %>
    <link rel="shortcut icon" href="http://handcannononline.com/favicon.ico" />
    <link rel="icon" type="image/vnd.microsoft.icon" href="http://handcannononline.com/favicon.ico" />
  </head>
  <body>
    <div id="container">
      <div id="content-header">
        <div class="logo">
          <h1><a href="http://handcannononline.com">Hand Cannon Online</a></h1>
        </div>
        <div id="flash">
          <%= render "layouts/errors"%>
        </div>
        
        <%= render "layouts/dashboard" %>
      </div>
      
      
      
      <%#= content_tag :h1, yield(:title) if show_title? %>
      <div id="content-body">
        <%= yield %>
      </div>    
    </div>
  </body>
</html>
